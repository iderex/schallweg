<!-- Keep the headings. Delete the guidance under them as you fill each one in. -->

Closes #

## What changed and what failure it prevents

<!-- What the change does, and the specific thing that goes wrong without it.
     "Improves X" is not a failure. "A missing band is read as zero" is. -->

## The means, and why it fits

<!-- One sentence naming what this is made of, the language, the format or the
     tool, and why that fits what this repository has to do. Asked every time
     rather than carried over from the last change. -->

## Commands behind the numbers

<!-- Every number in this body carries the command that produced it, run at the
     commit being pushed rather than in a working tree nobody else has. Paste
     the command and its output. If there are no numbers, say so. -->

## What this does not do

<!-- The scope you did not cover, the checks you did not run, the cases you did
     not test. A negative disclosure stays negative through every later edit of
     this body. -->

## Sign-off

<!-- Every commit needs a Signed-off-by line matching its author. See DCO and
     CONTRIBUTING.md. `git commit -s` adds it. -->
